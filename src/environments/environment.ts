// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAPTQsShZcQPgJTuTjLvYiewfRZGsqcilM",
    authDomain: "dominicmarra-95b72.firebaseapp.com",
    databaseURL: "https://dominicmarra-95b72.firebaseio.com",
    projectId: "dominicmarra-95b72",
    storageBucket: "dominicmarra-95b72.appspot.com",
    messagingSenderId: "135299901252",
    appId: "1:135299901252:web:bc001669f3556a46425911",
    measurementId: "G-88R7ZXTP9T"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
