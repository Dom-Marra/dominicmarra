import { ActiveFragmentDirective } from './active-fragment.directive';

describe('ActiveFragmentDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveFragmentDirective();
    expect(directive).toBeTruthy();
  });
});
